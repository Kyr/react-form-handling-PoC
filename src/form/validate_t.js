/**
 * Simple validation function.
 * @param {*} values
 * @return {isValid, errors[]}
 */
export default function validate(values, options) {
  console.log("Succes validation placeholder. Options: ", options);

  return { isValid: true, errors: [] };
}
